// MobileTopBar consolidated into TopBar. Keep this file as an alias so imports
// that still reference MobileTopBar continue to work while using a single
// implementation in `TopBar.tsx`.
export { default } from "./topBar"
