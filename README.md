Tech: Next.js, React, TypeScript, Tailwind CSS.

Quick start

1. Install dependencies: `npm install`
2. Run dev: `npm run dev` (starts on port 3001)
3. Build & start: `npm run build` then `npm start` (starts on port 3001)

Project structure: see the `app/` folder for pages and components.

License: MIT
